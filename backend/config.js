module.exports = {
  FRONTEND_URL: process.env.FRONTEND_URL || "http://localhost:3000", // Reemplaza con la URL correcta de tu frontend
  PORT: process.env.PORT || 4000, // Cambiado a 4000 para que coincida con el puerto en tu código
};
